import { h } from 'preact';
import styled from 'styled-components';

const InnerGroupWrapper = styled.div`
	background-color: #fff;
	border-radius: 2px;
	box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.4);
`;

export default InnerGroupWrapper;