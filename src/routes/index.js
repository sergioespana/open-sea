import { h } from 'preact';
import Container from '../components/Container';

const Home = () => (
	<div id="main">
		<Container>
			<h1>Home</h1>
			<p>This is the Home component.</p>
		</Container>
	</div>
);

export default Home;
