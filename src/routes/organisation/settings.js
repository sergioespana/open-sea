import { h } from 'preact';
import Container from '../../components/Container';

const Settings  = (props, { mobxStores: { store } }) => (
	<Container>
		<h1>Settings</h1>
	</Container>
);

export default Settings;